DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=changeme
DB_NAME=unite

# Configuración de S3
AWS_REGION=us-east-1
AWS_S3_BUCKET=nombre-del-bucket
AWS_S3_PREFIX=eventos
# Establecer en true solo si se habilitó acceso público en el bucket.
AWS_S3_PUBLIC_READ=false
# Opcional: usar un dominio público/cdn en lugar de la URL por defecto de AWS.
# AWS_S3_PUBLIC_URL=https://mi-dominio-publico

# Credenciales (usar variables del entorno del sistema si se prefiere no colocarlas aquí)
AWS_ACCESS_KEY_ID=CAMBIA_ME
AWS_SECRET_ACCESS_KEY=CAMBIA_ME_TAMBIEN
